```bash
npm create vite@latest project-name -- --template react

cd project-name
npm install
npm run dev
```


## 1. `QuizData.jsx`

Ez tartalmazza a kérdéseket, válaszokat.

```jsx
const QuizData = [
  {
    question: "Melyik évben jelent meg a React?",
    options: ["2010", "2013", "2015", "2017"],
    answer: "2013",
  },
  {
    question: "Melyik hook-ot használjuk state kezelésére?",
    options: ["useFetch", "useState", "useClass", "useReducer"],
    answer: "useState",
  },
  {
    question: "Mi a JSX?",
    options: [
      "JavaScript XML",
      "JSON for React",
      "CSS framework",
      "Backend nyelv"
    ],
    answer: "JavaScript XML",
  },
];

export default QuizData;
```

---

## 2. `Quiz.jsx`

Ez maga a quiz logikája.

```jsx
import React, { useState } from "react";
import QuizData from "../Data/QuizData";
import QuizResult from "./QuizResult";

function Quiz() {
  const [currentQ, setCurrentQ] = useState(0);
  const [score, setScore] = useState(0);
  const [isFinished, setIsFinished] = useState(false);

  const handleAnswer = (option) => {
    if (option === QuizData[currentQ].answer) {
      setScore(score + 1);
    }

    const nextQ = currentQ + 1;
    if (nextQ < QuizData.length) {
      setCurrentQ(nextQ);
    } else {
      setIsFinished(true);
    }
  };

  return (
    <div className="quiz-container">
      {isFinished ? (
        <QuizResult score={score} total={QuizData.length} />
      ) : (
        <div className="question-card">
          <h2>{QuizData[currentQ].question}</h2>
          <div className="options">
            {QuizData[currentQ].options.map((opt, index) => (
              <button key={index} onClick={() => handleAnswer(opt)}>
                {opt}
              </button>
            ))}
          </div>
          <p>
            Kérdés {currentQ + 1} / {QuizData.length}
          </p>
        </div>
      )}
    </div>
  );
}

export default Quiz;
```

---

## 3. `QuizResult.jsx`

Ez jeleníti meg az eredményt.

```jsx
import React from "react";

function QuizResult({ score, total }) {
  return (
    <div className="result-container">
      <h2>🎉 Quiz vége!</h2>
      <p>Elért pontszám: {score} / {total}</p>
      <button onClick={() => window.location.reload()}>Újra próbálom</button>
    </div>
  );
}

export default QuizResult;
```

---

## 4. `App.jsx`

A fő app, ami behívja a Quiz-t.

```jsx
import React from "react";
import Quiz from "./components/Quiz";

function App() {
  return (
    <div className="App">
      <h1>React Quiz App 🧠</h1>
      <Quiz />
    </div>
  );
}

export default App;
```

---

## 5. `main.jsx`

React gyökér indítás.

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

---

## 6. `index.css`

Egy kis styling:

```css
body {
  font-family: Arial, sans-serif;
  background: #f4f4f9;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.App {
  text-align: center;
}

.quiz-container, .result-container {
  background: white;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

button {
  margin: 0.5rem;
  padding: 0.7rem 1.2rem;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  background: #007bff;
  color: white;
  transition: 0.3s;
}

button:hover {
  background: #0056b3;
}
```

Tagek:
#WebDevelopment #FrontendDevelopment #Reactjs 

## Következő:
[[02.React projekt buildelése Hostingra]]