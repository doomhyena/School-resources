Adatbázis létrehozása:

```sql
CREATE DATABASE MyDB;
```

Adatbázis használata:

```sql
USE MyDB;
```

READ ONLY MODE

```sql
ALTER DATABASE MyDB READ ONLY = 1;
```

Tábla létrehozása:

```sql
CREATE TABLE employees (
	employee_id INT,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    hourly_pay DECIMAL(5, 2),
    hire_date DATE
);
```

DROP táblák:

```sql
DROP TABLE employees;
```

12:55 - 13:30

Tábla átnevezés:

```sql
RENAME TABLE employees TO workers;
```

Tábla hozzáadása:

```sql
ALTER TABLE employees
ADD phone_number VARCHAR(15);
```

Kijelölés:

```sql
SELECT * FROM employees;
```

Column átnevezése:

```sql
ALTER TABLE employess
RENAME COLUMN phone_number TO email;
```

Ezmi?? Át lehet rakni az oszlopot máshova (az utolsó sorral):

```sql
ALTER TABLE employees 
MODIFY COLUMN email VARCHAR(100);
AFTER last_name;

SELECT * FROM employees;
```

Oszlop törlése:

```sql
ALTER TABLE employees
DROP COLUMN email;

SELECT * FROM employees;
```

Érték hozzáadása

```sql
INSERT INTO employees
VALUES(1, "Eugene", "Krabs", 25.50, "2023-01-02");

SELECT * FROM employees;
```

Több hozzáadása

```sql
INSERT INTO employees
VALUES(2, "Squidward", "Tentacles", 15.00, "2023-01-03")
			(3, "Spongebob", "Squarepants", 12.50, "2023-01-04")
			(4, "Patrick", "Star", 12.50, "2023-01-05")
			(5, "Sandy", "Cheeks", 17.25, "2023-01-06");
			
SELECT * FROM employees;			
```

Hiányzó adatokkal

```sql
INSERT INTO employees(employee_id. first_name. last_name)
VALUES(6, "Sheldon", "Plankton");

SELECT * FROM employees;
```

Összes oszlop kiíratása

```sql
SELECT * FROM employees;
```

Egy specifikus oszlop kiíratása

```sql
SELECT first_name,last_name FROM employees;
```

Keresés:

```sql
SELECT * 
FROM employees
WHERE employee_id = 1;
```

Frissítés (1):

```sql
UPDATE employees
SET hourly_pay = 10.25
WHERE employee_id = 6;

SELECT * FROM employees;
```

Összes:

```sql
UPDATE employees
SET hourly_pay = 10.25

SELECT * FROM employees;
```

Törlés (aZ EGÉSZ TÁBLA)

```sql
DELETE employees
SELECT * FROM employees;
```

Törlés (1 vlmit):

```sql
UPDATE employees
WHERE employee_id = 6;

SELECT * FROM employees;
```
[00:37:03](https://www.youtube.com/watch?v=5OdVJbNCSso&list=PLD576VO0mmrgwoX4q4dk9TrsdQNUZwCaO&index=1&t=2223s) AUTOCOMMIT, COMMIT, ROLLBACK
## Órai első
[[01.SQL - Nobel]]

Tagek:
#MySQL #DatabaseManagement #SQL #DataAnalysis #BackendDevelopment