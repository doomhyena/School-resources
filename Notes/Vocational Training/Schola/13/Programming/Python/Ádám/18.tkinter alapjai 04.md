```python
#Csúszkák, csíkok, progress bar:
import tkinter as tk
from tkinter import ttk
from tkinter import scrolledtext

ablak4 = tk.Tk()
ablak4.title("Csúszkák")
#---
csúszkaSegéd = tk.DoubleVar(value=15)
csuszka = ttk.Scale(
    master=ablak4,
    command=lambda value: pb.stop(),
    from_ = 0,
    to = 25,
    length = 300,
    orient='horizontal',
    variable=csúszkaSegéd
)
csuszka.pack()
#---
#ProgressBar:
pb = ttk.Progressbar(
    master=ablak4,
    variable=csúszkaSegéd,
    maximum=25,
    orient='horizontal',
    mode='indeterminate',
    length=400
)
pb.pack()
#-------------
gyakInt = tk.IntVar()
gyakPB = ttk.Progressbar(
    master=ablak4,
    orient='vertical',
    variable=gyakInt
)
gyakPB.pack()
gyakPB.start()
gyakCimke = ttk.Label(master=ablak4,textvariable=gyakInt)
gyakCimke.pack()

#Csúszka, ami követi a töltődő ProgrssBar értékét:
gyakCsuszka = ttk.Scale(master=ablak4,variable=gyakInt, from_=0,to=100)
gyakCsuszka.pack()
#----------------------------------
ablak4.mainloop()
```
## Előző:
[[17.tkinter alapjai 03]]
## Következő
[[19.tkinter alapjai 06]]

Tagek:
#Python #Programming